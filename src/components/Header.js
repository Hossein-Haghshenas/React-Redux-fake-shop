const Header = () => {
  return (
    <header className="ui fixed menu">
      <section className="ui container center">
        <h2>USA Shop</h2>
      </section>
    </header>
  );
};

export default Header;
